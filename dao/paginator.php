<?php

//global variables


// initialize paginator
function paginator_update($records, $rows)
{
    global $total_rows;
    global $no_of_records_per_page;
    global $total_pages;
    global $pageno;
    global $offset;

}
